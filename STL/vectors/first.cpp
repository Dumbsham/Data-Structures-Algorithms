#include <bits/stdc++.h>
using namespace std;

int main()
{
    // pair <int, int> p ={1,3};
    // cout << p.first << " " << p.second;

    // storing more than two variables in a pair

    // pair <int ,pair<int,int>> p = {1,{3,4}};

    // cout << p.second.first << " " << p.second.second;

    pair<int, int> arr[] = {{1, 2}, {3, 4}, {5, 6}};

    cout << arr[1].second;
    return 0;
}